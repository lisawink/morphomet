name: morphoclim
channels:
  - conda-forge
dependencies:
  - python=3.11

  # core numerical stack
  - numpy
  - scipy
  - pandas

  # geospatial stack
  - geopandas
  - shapely
  - rasterio
  - rasterstats
  - osmnx
  - libpysal
  - momepy
  - r-suncalc

  # visualization
  - matplotlib
  - seaborn
  - plotly

  # stats / ML
  - statsmodels
  - scikit-learn

  # utilities
  - tqdm

  # pip-only packages
  - pip
  - pip:
      - geoplanar
      - neatnet